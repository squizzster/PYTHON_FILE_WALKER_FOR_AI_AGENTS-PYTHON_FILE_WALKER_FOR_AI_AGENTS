[project]
name = "python-file-walker-for-ai-agents"
version = "0.1.0"
description = "Experimental Python file traversal tooling for AI agents"
readme = "README.md"
authors = [
    { name = "Mark", email = "squizzster@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
python-file-walker-for-ai-agents = "python_file_walker_for_ai_agents.directory_tree:cli"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
